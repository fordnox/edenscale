# Phase 05: Backend — Documents, Communications, Tasks, Notifications, Audit Log

This phase finishes the backend by implementing the supporting tables: documents (file metadata; actual storage uses presigned URLs from a future blob provider but the API contract is in place), communications (broadcast letters/announcements with per-recipient delivery + read receipts), tasks, notifications, and the audit log. By the end of this phase, every table in `db.dbml` has a working CRUD API and the audit log captures every mutation through a small SQLAlchemy event listener.

## Tasks

- [x] Read Phase 02-04's repositories and routers as the pattern reference; do not invent new layering
  - Confirmed layering: thin routers (auth via `Depends(get_current_user_record)` or `require_roles(...)` from `app/core/rbac.py`) → `Repository(db: Session)` classes that own all DB I/O and status-transition logic → Pydantic schemas with `ConfigDict(from_attributes=True)` and split `Create`/`Update`/`Read` models. Sub-routers (e.g. `fund_capital_calls_router`) are exported from the same module and mounted in `app/main.py` with their own prefix. RBAC pattern: admin sees all; fund_manager scoped by `organization_id`; LP scoped via `InvestorContact.user_id` joined through commitments. Phase 05 modules will follow these conventions exactly.

- [x] Implement Documents:
  - `backend/app/schemas/document.py` — `DocumentCreate` (organization_id, fund_id, investor_id, document_type, title, file_name, mime_type, file_size, is_confidential), `DocumentUpdate`, `DocumentRead`, plus `DocumentUploadInit` (`file_name`, `mime_type`, `file_size`) and `DocumentUploadInitResponse` (`upload_url`, `file_url`, `expires_at`)
  - `backend/app/services/storage.py` — abstract `StoragePort` with `presign_put(key, mime_type) -> (upload_url, public_url, expires_at)` and `presign_get(key) -> url`. Provide a `LocalDevStorage` implementation that returns `http://localhost:8000/dev-storage/{key}` URLs and persists uploaded bytes under `backend/dev_storage/`. Add a tiny `POST /dev-storage/{key}` route protected by a dev-only header (skip in tests). Wire `StoragePort` selection through `settings.STORAGE_BACKEND`
  - `backend/app/repositories/document_repository.py` and `backend/app/routers/documents.py` — `POST /documents/upload-init`, `POST /documents` (records metadata after the client uploads to the presigned URL), `GET /documents`, `GET /documents/{id}` (returns a fresh presigned GET url), `PATCH /documents/{id}`, `DELETE /documents/{id}`. RBAC: confidential docs visible only to admin/fund_manager in same org and LPs whose `investor_id` matches. Mount under `/documents`
  - Notes: implemented with two settings (`STORAGE_BACKEND="local"`, `DEV_STORAGE_TOKEN`) and a singleton `get_storage()` selector. `LocalDevStorage` writes to `${APP_DATA_PATH}/dev_storage/`. The dev-storage PUT/POST route lives on a separate `dev_storage_router` (no JWT auth), guarded by an `x-dev-storage-token` header and gated behind `LocalDevStorage` so it 404s in S3-backed deploys. `GET /documents/{id}` returns a fresh presigned GET via `download_url` alongside the canonical `file_url`. RBAC: admin sees all; fund_manager sees docs scoped to its org/funds plus its own uploads; LP sees docs scoped to investors they're a contact for, their own uploads, and non-confidential docs on funds where they hold a commitment. Initial test file `tests/test_documents_api.py` covers happy-path upload + get round trip, dev-storage auth, LP/FM RBAC failures, and patch/delete lifecycle (the broader Phase 05 test sweep is still owned by the dedicated tests checkbox below).

- [x] Implement Communications + Recipients:
  - Schemas/repository/router with endpoints `GET /communications`, `GET /communications/{id}`, `POST /communications` (drafts), `PATCH /communications/{id}`, `POST /communications/{id}/send` (sets `sent_at` and creates one `communication_recipients` row per resolved recipient), `POST /communications/{id}/recipients/{recipient_id}/read` (sets `read_at`)
  - Recipient resolution helper: given a fund_id, expand to all primary investor_contacts whose investors hold an approved commitment in that fund. Allow explicit overrides via the request body `recipients: list[{ user_id?: int, investor_contact_id?: int }]`
  - Mount under `/communications`. Add nested `GET /funds/{fund_id}/communications`
  - Notes: `CommunicationRepository.resolve_default_recipients(fund_id)` walks `Commitment` (status=approved) → `InvestorContact` (is_primary=True) and returns `(user_id, investor_contact_id)` pairs; `send()` accepts optional `explicit_recipients` overrides, dedupes pairs, stamps `sent_at` and bulk-inserts recipient rows. Once sent, edits/sends are 409. RBAC: admin sees all; FM scoped by sender or fund's organization (manage limited to admin / FM in same org / sender); LP only sees comms where they are a recipient (directly via `user_id` or transitively via their `InvestorContact`). LP `/recipients/{id}/read` enforces ownership of the row. Tests: `tests/test_communications_api.py` — 11 cases covering primary-contact resolution, explicit-override, double-send 409, no-recipients 409, post-send PATCH 409, recipient read marking, three RBAC failures, and the nested `/funds/{fund_id}/communications` route.

- [x] Implement Tasks:
  - Schemas/repository/router for `GET /tasks`, `GET /tasks/{id}`, `POST /tasks`, `PATCH /tasks/{id}`, `POST /tasks/{id}/complete` (sets status=done and `completed_at=now()`)
  - Default list filter: `assigned_to_user_id = current_user.id`. Query parameters `?fund_id=`, `?status=`, `?assignee=` for fund_managers/admins
  - Mount under `/tasks`. Add nested `GET /funds/{fund_id}/tasks`
  - Notes: `TaskRepository.list_for_user` enforces RBAC: admin sees all (optionally filtered by `assignee`); FM sees tasks on funds in their org plus tasks they created or are assigned to; LP only sees tasks assigned to them. The router applies the "default to my own tasks" filter only when the caller is admin/FM AND no `fund_id`/`status_filter`/`assignee` query parameters are set, so explicit filters always win. POST/PATCH gated to admin/FM (with FM scoped to own-org funds + own-created tasks); `/complete` additionally allows the assigned LP to flip their own task to done. PATCHing `status=done` mirrors the explicit `/complete` endpoint by stamping `completed_at`; reverting away from done clears it. Cancelled tasks 409 on `/complete`. Tests: `tests/test_tasks_api.py` — 16 cases covering happy-path create/complete (incl. idempotency), PATCH→done timestamp behaviour, default-vs-filtered listing, FM cross-org and LP create RBAC failures, LP assignee-only completion, and the nested `/funds/{fund_id}/tasks` route.

- [x] Implement Notifications:
  - Schemas/repository/router for `GET /notifications` (defaults to current user, sorted desc by `created_at`), `POST /notifications/{id}/read`, `POST /notifications/{id}/archive`, `POST /notifications/read-all`
  - Add `notification_service.notify(user_id, title, message, related_type=None, related_id=None)` and call it from key business events: capital_call sent, distribution sent, communication sent, task assigned to a different user
  - Mount under `/notifications`
  - Notes: `NotificationRead` schema is read-only (notifications are server-emitted, not user-created). `NotificationRepository.mark_read` is idempotent — only the unread→read transition stamps `read_at`; `mark_archived` is similarly idempotent. `mark_all_read` uses a bulk UPDATE returning the row count via `NotificationsReadAllResponse{ updated: int }`. RBAC: every endpoint operates on `current_user.id` only; `/{id}/read` and `/{id}/archive` 403 when the row belongs to another user. `notification_service.notify(db, user_id=None, ...)` is a fire-and-forget wrapper that no-ops on `user_id is None` so unresolved investor-contact recipients don't crash the parent flow. Fan-out wired into: communications send (per recipient, deduped by user_id, walking `InvestorContact` when only the contact is set); capital-call send (primary contacts of approved commitments on items, distinct user_ids); distribution send (same shape as capital calls); task create + PATCH (notifies the new assignee only when they differ from both the previous assignee and the actor — self-assigns stay silent). Tests: `tests/test_notifications_api.py` — 12 cases covering listing/desc-order, status filter, mark-read, archive, read-all (only flips unread, leaves archived intact), 403/404 paths, plus fan-out for communication send, task create, task self-assign no-op, and PATCH reassignment.

- [x] Implement the Audit Log:
  - Add `backend/app/core/audit.py` exporting `record_audit(db, user, action, entity_type, entity_id, metadata=None, request=None)` and an SQLAlchemy `after_insert` / `after_update` / `after_delete` event listener registered for the entities that matter (organizations, users, funds, commitments, capital_calls, distributions, documents, communications, tasks). The listener writes a row to `audit_logs` capturing the actor (read from a request-scoped context var set by a FastAPI middleware), the action verb, entity type/id, and a JSON-encoded diff in `metadata`
  - Add `backend/app/middleware/audit_context.py` middleware that puts the current user_id and ip_address into a `contextvars.ContextVar` for the duration of the request
  - Expose `GET /audit-logs` (admin-only) and `GET /audit-logs?entity_type=fund&entity_id=...` for tracing
  - Mount under `/audit-logs`
  - Notes: `AuditContextMiddleware` initialises a fresh `AuditContext(user_id=None, ip_address=request.client.host)` per request; `get_current_user_record` backfills `user_id` once auth resolves the local row. The mapper-level event listeners use `connection.execute(AuditLog.__table__.insert().values(...))` to write inside the same transaction as the originating change (so a rollback discards both); the SQL column is named `metadata` (the ORM attribute is `audit_metadata` to avoid clashing with `Base.metadata`) — Core inserts must key by the column name. `_build_diff` walks `state.attrs[...].history` for column attributes only and skips `created_at`/`updated_at` so onupdate-driven no-op writes don't emit empty rows. JSON serialiser handles datetime/Decimal/Enum via `default=_json_default`. Listeners attach to Organization, User, Fund, Commitment, CapitalCall, Distribution, Document, Communication, Task. `record_audit` exists for non-mutation events (e.g. logins) and commits its own row. `GET /audit-logs` is gated by `require_roles(UserRole.admin)` and supports `entity_type`/`entity_id`/`user_id`/`action`/skip/limit filters. Tests: `tests/test_audit_log.py` — 11 cases covering create/update/delete listener emission, diff payload shape, no-op-update suppression, actor pulled from ContextVar, `record_audit` helper, admin listing, entity-filter, FM/LP 403s, and the empty-context default.

- [x] Update Dashboard overview with new aggregates:
  - Surface `unread_notifications_count`, `open_tasks_count` for the current user, and `recent_communications` (5 most recent) so the Dashboard page has all the numbers it needs
  - Notes: extended `DashboardOverviewResponse` with three new fields and a `CommunicationSummary` schema (id, fund_id, sender_user_id, type, subject, sent_at, created_at) — kept light so the dashboard doesn't pull recipient rows. `unread_notifications_count` filters `Notification.user_id == current_user.id` AND `status == unread`. `open_tasks_count` is scoped to `Task.assigned_to_user_id == current_user.id` with `status IN (open, in_progress)` — completed/cancelled tasks are excluded so the count reflects actionable work. `recent_communications` reuses `CommunicationRepository`'s visibility scoping via a new `list_recent_for_user` method that also filters to `sent_at IS NOT NULL` and orders by `sent_at DESC` so drafts never leak and the most recent activity surfaces first. Tests: `tests/test_dashboard.py::TestDashboardActivityAggregates` — 4 new cases covering notification count (only current user, only `unread` status), open-tasks count (only current user assignments, excludes done/cancelled), recent-communications draft suppression + 5-item cap + sent_at desc ordering, and LP visibility scoping. Existing `test_no_user_row_returns_zeros` extended to assert the three new zero/empty defaults.

- [ ] Add focused integration tests covering the new modules:
  - `backend/tests/test_documents_api.py`, `test_communications_api.py`, `test_tasks_api.py`, `test_notifications_api.py`, `test_audit_log.py` — at least one happy path and one auth/RBAC failure per module
  - For documents, use the LocalDevStorage backend in tests so the round-trip works without external dependencies

- [ ] Sync OpenAPI and run gates:
  - Run `make openapi`, `make test`, `make lint`, fix findings
  - Confirm `frontend/src/lib/schema.d.ts` exposes every new endpoint
